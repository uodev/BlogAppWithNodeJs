<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Giriş Yap</title>
    <link rel="stylesheet" href="/css/style.css" />
    <!-- CSS only -->
    <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65"
            crossorigin="anonymous"
    />
</head>
<body class="bg-dark">
<div class="container-max-700 center-flex" style="padding-top: 10%">
    <div <%= userRegistered ? '' : `style=display:none;` %> class="alert alert-success">Başarıyla kayıt olundu!</div>
    <div <%= message === 'wrong password' ? '' : `style=display:none;` %> class="alert alert-danger">Şifreniz Hatalı</div>
    <div <%= message === 'user not found' ? '' : `style=display:none;` %> class="alert alert-danger">Kullanıcı Bulunamadı!</div>
    <h3 style="color: white">Giriş Yap</h3>
    <form action="/admin/user-login" method="post">
        <br>
        <div>
            <label style="color: white" for="username">Kullanıcı Adı</label>
            <br>
            <input type="text" name="userName" id="username" placeholder="example" required >
        </div>
        <br>
        <div>
            <label for="password" style="color: white">Şifre</label>
            <br>
            <input type="password" name="password" id="password" placeholder="example" required >
        </div>
        <br>

        <button class="btn btn-success" type="submit">Giriş Yap</button>
    </form>
    <br>
    <a href="/admin/user-register" class="a-dark">Henüz kayıt olmadın mı?</a>
</div>
</body>
</html>